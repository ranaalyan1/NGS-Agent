# Swarm pipeline host deps (cli.py + worker.py). Version floors match the
# `swarm` extra in pyproject.toml — keep them in sync.
temporalio>=1.8.0
redis==5.0.1
boto3==1.34.0
pydantic==2.5.0
click==8.1.7
python-dotenv==1.0.0
anthropic==0.48.0
pytest==8.3.3
jinja2==3.1.4
